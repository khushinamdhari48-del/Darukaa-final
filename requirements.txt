# --- core runtime (required) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pyyaml>=6.0
numpy>=1.26
python-dotenv>=1.0
rich>=13.7
httpx>=0.27

# --- LLM layer (optional at runtime: system degrades to the deterministic
#     reasoning engine when ANTHROPIC_API_KEY is absent) ---
anthropic>=0.40

# --- tests ---
pytest>=8.0
